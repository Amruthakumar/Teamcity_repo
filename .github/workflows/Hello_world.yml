name: CI pipeline

on:
  push:
    branches: [master]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-lastest
    
    steps:
      - uses: actions/checkout@v4
      - name: run the foloowing on
        run: echo hello world!
